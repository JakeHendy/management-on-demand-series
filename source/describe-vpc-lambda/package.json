{
  "name": "describe-vpc-lambda",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "aws-sdk": "^2.814.0"
  },
  "devDependencies": {},
  "scripts": {
    "build": "tsc -p ./; zip -r dist.zip ./dest/",
    "start": "npm run build -- -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
